- Default configuration options? Is there a way to do it without behaving unpredictably?
-- The main issue is that defaults are defaults, and if they're on they are harder to find to turn off
-- For users at least, we could have defaults and then only enable applicable services, and then allow overrides as well
- Make better nushell aliases and library calls for it
